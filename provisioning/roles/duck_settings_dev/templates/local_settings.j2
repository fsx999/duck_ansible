# coding=utf-8
import re
EMAIL_HOST = "{{ EMAIL_HOST }}"
EMAIL_USE_TLS = "{{ EMAIL_USE_TLS }}"
EMAIL_PORT = {{ 2525 }}
EMAIL_HOST_USER = "{{ EMAIL_HOST_USER }}"
EMAIL_HOST_PASSWORD = "{{ EMAIL_HOST_PASSWORD }}"
DEBUG = True

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': '{{ postgres_database }}',                      # Or path to database file if using sqlite3.
        'USER': '{{ postgres_user }}',                      # Not used with sqlite3.
        'PASSWORD': '{{ postgres_password }}',                  # Not used with sqlite3.
        'HOST': '{{ postgres_host }}',                      # Set to empty string for localhost. Not used with sqlite3.
        'PORT': '',                      # Set to empty string for default. Not used with sqlite3.

    },
    'oracle': {
        'ENGINE': 'django.db.backends.oracle',
        'NAME': '{{ oracle_name }}',
        'USER': '{{ oracle_user }}',
        'PASSWORD': '{{ oracle_password }}',
        'HOST': '{{ oracle_host }}',
        'PORT': '{{ oracle_port }}',
    },
}

COMPOSANTE = '{{ COMPOSANTE }}'
INTERNAL_IPS = ('127.0.0.1',)
DEV_APPS = ('devserver', 'debug_toolbar')
WKHTMLTOPDF_CMD = '{{ WKHTMLTOPDF_CMD }}'
